# Open desk follow-ups

Left after the tile pass. Do not fold these into the phone-classification branch.

- Put Open a folder in the header, on the right, so it stays put. The tall dashed plate returns only when the desk is empty.
- Keep Continue as the only full-width plate. Other shoots go in the grid.
- Use the washed photograph on Continue only.
- Let the title and section labels scroll. Do not pin them.
- The cover frame uses the stored sharpness and exposure score. A subjective cover waits on the taste-model gate.
- Harness folders under Application Support should stop being written by tests, and the copies already there can be removed.
- A folder just pointed at should show its frames before the table.
