#!/usr/bin/env python3
"""STUB (F2): Live one-⌘Z-per-gesture asserts — Swift-on-macOS REQUIRED.

owned-by-CP4 — exercises P0Command undo through the running app + probe.
"""
from __future__ import annotations

import sys


def main() -> int:
    print(
        "STUB: run_live_undo.py — live undo asserts not wired yet "
        "(owned-by-CP4). Refusing vacuous PASS.",
        file=sys.stderr,
    )
    return 1


if __name__ == "__main__":
    raise SystemExit(main())
