#!/usr/bin/env python3
"""STUB (F2): Live count invariants (banner=header=receipt) — Swift-on-macOS REQUIRED.

owned-by-CP7 — A1 invariant against live staging chrome via Probe v2.
"""
from __future__ import annotations

import sys


def main() -> int:
    print(
        "STUB: run_live_invariants.py — live count invariants not wired yet "
        "(owned-by-CP7). Refusing vacuous PASS.",
        file=sys.stderr,
    )
    return 1


if __name__ == "__main__":
    raise SystemExit(main())
